-Find a way so that gd.sh deals not only with newline-separated
 streams, but also with binary streams.
 This is a problem, in particular for sed.
-Resuse some of the scripts written down here.
-Change the use of gd.sh to comm.
-Write a man-page (or several).
-Write the following C-programs: -pol (print only links)
				 -pot (print only text)
 Use them where they are needed.
